#!/usr/bin/env bash
# ============================================================================
#  一键: 编译 + 运行 (需要访问 /dev/spidev /dev/i2c /dev/gpiochip, 故用 sudo)
#  用法: ./run.sh
# ============================================================================
set -e

cd "$(dirname "$0")"

echo "==> 编译 ..."
make

echo "==> 启动实时仪表盘 (Ctrl-C 退出) ..."
sudo ./sensor
